import { FormatPeerCountPipe } from './format-peer-count.pipe';

describe('FormatPeerCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPeerCountPipe();
    expect(pipe).toBeTruthy();
  });
});
